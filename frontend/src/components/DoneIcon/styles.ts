import styled from 'styled-components';
import * as C from '@/shared/ui/assets/styles/components';
import doneIcon from '@assets/images/done.svg';

export const DoneIcon = styled(C.Icon)`
  background-image: url(${doneIcon});
`;
