import styled from 'styled-components';
import * as C from '@styles/components';

export const CourseWrapper = styled(C.FlexContainer)`
  align-items: center;
  height: 63px;
  padding: 0 20px 0 7px;
`;
